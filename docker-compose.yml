services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: karate
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - karate_db_data:/var/lib/postgresql/data

volumes:
  karate_db_data:
